/* ---- Homework Problem 11.8 (**) --------------------------------------------
 * Modify Tiny so that it reaps CGI children inside a SIGCHLD handler instead
 * of explicitly waiting for them to terminate.
 * ------------------------------------------------------------------------- */
